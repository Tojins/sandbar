---
# The operator's public key is an invocation-time value on the bootstrap channel
# and box state afterwards (#146): an unattended `ansible-pull` has none of the
# operator's environment, and a key list that came out empty there would take
# every key off root. So the bootstrap's keys are remembered, and a pull with
# nothing supplied uses exactly what the last bootstrap set.
- name: Separate the operator keys this invocation supplied from empty placeholders
  ansible.builtin.set_fact:
    sandbar_supplied_ssh_keys: "{{ sandbar_operator_ssh_keys | reject('equalto', '') | list }}"

- name: Look for the operator keys the box remembers
  ansible.builtin.stat:
    path: "{{ sandbar_operator_keys_file }}"
  register: sandbar_remembered_keys_file

- name: Read the operator keys the box remembers
  ansible.builtin.slurp:
    src: "{{ sandbar_operator_keys_file }}"
  register: sandbar_remembered_keys
  when:
    - sandbar_supplied_ssh_keys | length == 0
    - sandbar_remembered_keys_file.stat.exists

- name: Take the operator keys from this invocation, or from the box's memory
  ansible.builtin.set_fact:
    sandbar_operator_ssh_keys: >-
      {{ sandbar_supplied_ssh_keys if sandbar_supplied_ssh_keys | length > 0
         else (sandbar_remembered_keys.content | default('') | b64decode).splitlines()
              | reject('equalto', '') | list }}

- name: Refuse to run without the host variables the role cannot default
  ansible.builtin.assert:
    that:
      - sandbar_installations is sequence
      - sandbar_installations is not string
      - sandbar_installations is not mapping
      - sandbar_installations | length > 0
      - sandbar_git_email | length > 0
      - sandbar_operator_ssh_keys | length > 0
    fail_msg: >-
      Set sandbar_installations and sandbar_git_email in group_vars, and run one
      bootstrap with SANDBAR_OPERATOR_SSH_KEY set so
      {{ sandbar_operator_keys_file }} exists. The key list is exclusive: a key
      not listed is removed from root and every installation user.

- name: Validate every installation inventory entry
  ansible.builtin.assert:
    that:
      - (item.user | default('')) is match('^[a-z_][a-z0-9_-]{0,31}$')
      - (item.user | default('')) != 'root'
      - (item.project | default('')) is match('^[A-Za-z0-9][A-Za-z0-9._-]*$')
      - (item.project | default('')) != 'installation'
      - item.clone_url | default('') | length > 0
      - (item.reader_port | default(none)) is integer
      - (item.reader_port | default(0) | int) >= 1
      - (item.reader_port | default(0) | int) <= 65535
    fail_msg: >-
      Every sandbar_installations entry needs a non-root safe Linux user, a
      project path component other than the reserved installation directory, a
      non-empty clone_url and an integer reader_port from 1 through 65535.
      Invalid entry: {{ item }}
  loop: "{{ sandbar_installations }}"
  loop_control:
    label: "{{ item.user | default('missing user') }}"

- name: Refuse installation identities, routes or reader ports that are not unique
  ansible.builtin.assert:
    that:
      - (sandbar_installations | map(attribute='user') | list | unique | length) == (sandbar_installations | length)
      - (sandbar_installations | map(attribute='project') | list | unique | length) == (sandbar_installations | length)
      - (sandbar_installations | map(attribute='reader_port') | list | unique | length) == (sandbar_installations | length)
    fail_msg: >-
      Every installation needs a unique Linux user, project route and
      reader_port; duplicates would overwrite one home and unit pair, route
      one prefix to multiple readers, or prevent readers binding concurrently.

- name: Refuse a host that is not Ubuntu 24.04 or 26.04
  ansible.builtin.assert:
    that:
      - ansible_facts['distribution'] == 'Ubuntu'
      - ansible_facts['distribution_version'] in sandbar_ubuntu_releases
    fail_msg: >-
      This role targets Ubuntu {{ sandbar_ubuntu_releases | join(' or ') }};
      this host is {{ ansible_facts['distribution'] }}
      {{ ansible_facts['distribution_version'] }}.

- name: Create the directory the remembered keys live in
  ansible.builtin.file:
    path: "{{ sandbar_operator_keys_file | dirname }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Remember the operator keys so the box can converge unattended
  ansible.builtin.copy:
    content: "{{ sandbar_operator_ssh_keys | join('\n') }}\n"
    dest: "{{ sandbar_operator_keys_file }}"
    owner: root
    group: root
    mode: "0644"

# Host-global tasks run once regardless of daemon count.
- name: Install host packages
  ansible.builtin.import_tasks: packages.yml

- name: Configure host podman prerequisites
  ansible.builtin.import_tasks: podman-global.yml

- name: Reconcile the AppArmor profiles rootless podman runs under
  ansible.builtin.import_tasks: apparmor.yml

- name: Provide swap
  ansible.builtin.import_tasks: swap.yml

- name: Restrict SSH
  ansible.builtin.import_tasks: ssh.yml

- name: Configure unattended upgrades
  ansible.builtin.import_tasks: upgrades.yml

- name: Configure Caddy installation routes
  ansible.builtin.import_tasks: caddy.yml

# Before every installation task: the units name the built driver, and an
# installation whose files or unit changed is asked to restart into it.
- name: Build the driver every installation runs
  ansible.builtin.import_tasks: driver.yml

- name: Create each installation account and podman session
  ansible.builtin.include_tasks: account.yml
  loop: "{{ sandbar_installations }}"
  loop_control:
    loop_var: sandbar_installation
    index_var: sandbar_installation_index
    label: "{{ sandbar_installation.user }}/{{ sandbar_installation.project }}"
  vars:
    sandbar_user: "{{ sandbar_installation.user }}"
    sandbar_project: "{{ sandbar_installation.project }}"
    sandbar_home: "/home/{{ sandbar_installation.user }}"
    sandbar_checkout: "/home/{{ sandbar_installation.user }}/{{ sandbar_installation.project }}"
    sandbar_installation_dir: "/home/{{ sandbar_installation.user }}/installation"
    sandbar_reader_port: "{{ sandbar_installation.reader_port }}"
    sandbar_installation_git_name: "{{ sandbar_installation.git_name | default(sandbar_git_name) }}"
    sandbar_installation_git_email: "{{ sandbar_installation.git_email | default(sandbar_git_email) }}"
    sandbar_subid_start: "{{ sandbar_subid_base + sandbar_installation_index * sandbar_subid_count }}"

# This separate pass guarantees every user and installation directory exists
# before the expected first-run refusal on a hand-placed sandbar.env.
- name: Prepare each installation directory
  ansible.builtin.include_tasks: prepare-installation.yml
  loop: "{{ sandbar_installations }}"
  loop_control:
    loop_var: sandbar_installation
    label: "{{ sandbar_installation.user }}/{{ sandbar_installation.project }}"
  vars:
    sandbar_user: "{{ sandbar_installation.user }}"
    sandbar_home: "/home/{{ sandbar_installation.user }}"
    sandbar_installation_dir: "/home/{{ sandbar_installation.user }}/installation"

- name: Configure each sandbar checkout and unit pair
  ansible.builtin.include_tasks: installation.yml
  loop: "{{ sandbar_installations }}"
  loop_control:
    loop_var: sandbar_installation
    label: "{{ sandbar_installation.user }}/{{ sandbar_installation.project }}"
  vars:
    sandbar_user: "{{ sandbar_installation.user }}"
    sandbar_project: "{{ sandbar_installation.project }}"
    sandbar_home: "/home/{{ sandbar_installation.user }}"
    sandbar_checkout: "/home/{{ sandbar_installation.user }}/{{ sandbar_installation.project }}"
    sandbar_installation_dir: "/home/{{ sandbar_installation.user }}/installation"
    sandbar_reader_port: "{{ sandbar_installation.reader_port }}"

# Last, and host-global: the timer counts from its own activation, so a fresh
# box's bootstrap finishes before the first unattended play can start (#146).
- name: Install the convergence timer that applies every later commit
  ansible.builtin.import_tasks: pull.yml
