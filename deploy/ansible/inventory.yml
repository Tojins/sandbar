---
# The bootstrap channel's one host; the operator's SSH config resolves
# `sandbar-box`. Installations, identities and paths are in group_vars/all.yml
# so the box's own `ansible-pull` reads the same data with no inventory file at
# all (#146).
all:
  hosts:
    sandbar-box:
      ansible_user: root
