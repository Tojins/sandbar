## This branch is part of a chunk

This issue is one member of a **chunk** — a group of related, review-gated
issues that land together on `{{chunkBranch}}` and get reviewed as one unit.
This branch was seeded from that branch's tip, not from `{{sourceBranch}}`.

So the commits and diff above are this issue's contribution alone: the earlier
members' work is under it, already on `{{chunkBranch}}`, and is not yours to
review here. Review what this issue added, against what the chunk already had.
Do not report the chunk's existing code as missing, and do not ask for it to be
restated on this branch.
