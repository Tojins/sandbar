A `git merge --no-ff` of this branch into the source branch is in progress and
has hit a conflict.

{{digest}}

Resolve every conflict, `git add` the files, and `git commit --no-edit` to
complete the merge. The orchestrator will then run the project's `check` +
`test` against the result.
