; Keep the existing release convention when running `npm version <type>`:
; commit subject "chore: bump version to X.Y.Z" and a lightweight `vX.Y.Z` tag
; (tag-version-prefix defaults to "v"). See README "Releasing".
message=chore: bump version to %s
